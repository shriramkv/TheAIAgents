version: '3.8'

services:
  mcp-server:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    restart: always

networks:
  default:
    name: mcp-network
